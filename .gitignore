node_modules/
dist/
build/
.env
.env.*
!.env.example
.DS_Store
Thumbs.db
*.log
*.sqlite
*.sqlite3
.seed-ae/
local-assets/
tmp/
.seed-watchdog.log

# Local reference material — look-dev plates, stills, anything gathered
# for a shot. Yours, not the product's.
reference/

# Plugin build output
plugins/*/build/
# A built .aex has been committed to the root twice now, once from a
# build run outside the plugin folder. It is 160KB of stale binary that
# nothing loads — installs come from plugins/*/build.
*.aex
# Debug artefacts from the thumbnail path.
_thumb.png

# Probe output: generated images, not source.
.seed-probes/

# Companion build output — regenerated by apps/installer/scripts/build.mjs
apps/installer/dist/
apps/installer/release/
apps/installer/resources/
apps/installer/build/
